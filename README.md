# Is Palindrome

A Python solution to check if a string can be a palindrome by deleting at most one character.

## Algorithm

Uses a two-pointer approach to detect mismatches and checks if either substring (after deleting one character) can form a palindrome.

## Running Tests

```bash
uv run pytest
```
